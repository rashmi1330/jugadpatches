<?php

/*
 * @file
 * code for First module
*/

/**
 * Implements hook_theme().
 */
function products_theme($existing, $type, $theme, $path) {
  return array(
      'products_qr_code_block' => array(
          'variables' => array(
              'url' => array(),
              'width' => array(),
              'height' => array(),
              'caption' => array(),
          ),
      ),
  );
}